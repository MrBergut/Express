<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= username.toUpperCase() %></title>
    <link rel="stylesheet" href="/css/user.css">
</head>
<body>
    <%- include('blocks/header') -%>
    <main>
        <h1>Страница пользователя: <%= username.toUpperCase() %></h1>
        <% if(username.toLowerCase() == 'john') { %>
        <ul>
        <% hobbies.forEach((el) => { %>
            <li><%= el %></li>
        <% }) %>
        </ul>
        <% } %>
    </main>
</body>
</html>
